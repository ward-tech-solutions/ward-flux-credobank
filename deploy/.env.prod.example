# ========================================================================
# CredoBank WARD OPS - Production Environment Configuration
# ========================================================================
# IMPORTANT:
# 1. Copy this file to .env.prod before deployment
# 2. Generate secure secrets (use deploy.sh for automatic generation)
# 3. Never commit .env.prod to version control
# ========================================================================

# ========================================================================
# DATABASE CONFIGURATION
# ========================================================================
# PostgreSQL connection string (uses internal Docker service name 'db')
DATABASE_URL=postgresql://fluxdb:FluxDB@db:5432/ward_ops

# ========================================================================
# REDIS CONFIGURATION (Task Queue & Cache)
# ========================================================================
# Redis connection string with password
REDIS_URL=redis://:RedisPass@redis:6379/0
# Standalone Redis password (must match the password in REDIS_URL above)
REDIS_PASSWORD=RedisPass

# ========================================================================
# SECURITY KEYS (CRITICAL - GENERATE UNIQUE VALUES)
# ========================================================================
# JWT signing secret - MUST be 32+ random characters
# Generate with: python3 -c "import secrets, base64; print(base64.urlsafe_b64encode(secrets.token_bytes(32)).decode())"
SECRET_KEY=CHANGE_ME_TO_RANDOM_32_PLUS_CHARACTERS

# Fernet encryption key for credential storage - MUST be valid Fernet key
# Generate with: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=CHANGE_ME_TO_FERNET_KEY

# ========================================================================
# ADMIN CREDENTIALS
# ========================================================================
# Initial admin password for first login (change immediately after deployment)
DEFAULT_ADMIN_PASSWORD=admin123

# ========================================================================
# APPLICATION SETTINGS
# ========================================================================
# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Monitoring mode: snmp_only, zabbix_only, or hybrid
MONITORING_MODE=hybrid

# CORS origins - comma-separated list or * for all
# Production: Use specific domain, e.g., https://ops.credobank.ge
# Development: Use * to allow all origins
CORS_ORIGINS=*

# ========================================================================
# OPTIONAL: VICTORIAMETRICS (Time-Series Metrics)
# ========================================================================
# Uncomment if using VictoriaMetrics for advanced metrics storage
# VICTORIA_URL=http://victoriametrics:8428

# ========================================================================
# OPTIONAL: ZABBIX INTEGRATION
# ========================================================================
# Configure via UI after deployment, or set here for pre-configuration
# ZABBIX_URL=https://your-zabbix-server.com/api_jsonrpc.php
# ZABBIX_USER=your-zabbix-username
# ZABBIX_PASSWORD=your-zabbix-password

# ========================================================================
# OPTIONAL: SNMP CREDENTIALS (Default)
# ========================================================================
# Default SNMP credentials for device discovery (can be overridden per device)
# WARD_ENCRYPTION_KEY=same-as-ENCRYPTION_KEY-or-separate-key
# DEFAULT_SNMP_COMMUNITY=public
# DEFAULT_SNMP_VERSION=2c

# ========================================================================
# DEPLOYMENT METADATA (Optional)
# ========================================================================
# CLIENT_NAME=CredoBank
# DEPLOYMENT_DATE=2025-10-12
# DEPLOYED_BY=ward-tech-solutions
