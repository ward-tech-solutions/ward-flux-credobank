# WARD TECH SOLUTIONS - Environment Configuration Template
# Copy to .env: cp .env.example .env

# Zabbix API
ZABBIX_URL=http://your-zabbix-server:8080/api_jsonrpc.php
ZABBIX_USER=your_username
ZABBIX_PASSWORD=your_password

# Security Keys (Generate with: openssl rand -hex 32)
SECRET_KEY=generate-with-openssl-rand-hex-32
JWT_SECRET_KEY=generate-with-openssl-rand-hex-32

# Database
# For SQLite (Development):
# DATABASE_URL=sqlite:///data/ward_ops.db

# For PostgreSQL (Production):
DATABASE_URL=postgresql://ward_admin:changeme_secure_password@localhost:5432/ward_ops
USE_POSTGRES=true

# PostgreSQL Connection Details (for Docker Compose)
POSTGRES_DB=ward_ops
POSTGRES_USER=ward_admin
POSTGRES_PASSWORD=changeme_secure_password

# Application
DEBUG=False
HOST=0.0.0.0
PORT=5001
WORKERS=4

# Security
CORS_ORIGINS=*
ALLOWED_HOSTS=localhost,127.0.0.1,wardops.tech,*.wardops.tech

# Rate Limiting
RATE_LIMIT_LOGIN=5/minute
RATE_LIMIT_API=100/minute

# Logging
LOG_LEVEL=INFO
LOG_FILE=logs/ward_ops.log
