# ═══════════════════════════════════════════════════════════════════
# WARD TECH SOLUTIONS - Python Dependencies
# ═══════════════════════════════════════════════════════════════════

# FastAPI and async dependencies
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
httpx==0.26.0
jinja2>=3.1.0

# Security
python-dotenv==1.0.0
slowapi==0.1.9
python-jose[cryptography]==3.3.0
passlib[argon2]==1.7.4
argon2-cffi==23.1.0
email-validator==2.1.0

# Database
sqlalchemy>=2.0.23
alembic>=1.13.1
psycopg2-binary>=2.9.9  # PostgreSQL adapter

# Standalone Monitoring Engine
pysnmp-lextudio>=6.1.0  # SNMP library (still maintained in 2025, despite deprecation warnings)
pyasn1>=0.5.1  # Required for SNMP
icmplib>=3.0.4  # Async ICMP (ping) library
celery[redis]>=5.3.4  # Distributed task queue
redis>=5.0.1  # Message broker and cache
flower>=2.0.1  # Celery monitoring UI
cryptography>=41.0.7  # Credential encryption
APScheduler>=3.10.4  # Advanced scheduling

# VictoriaMetrics Client
requests>=2.31.0  # HTTP client for VM API
prometheus-client>=0.19.0  # Prometheus-compatible metrics

# SSH Support
paramiko==4.0.0

# Excel/CSV processing
openpyxl==3.1.2
pandas>=2.1.4

# Testing (Development)
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0

# Code Quality (Development)
black==23.12.1
ruff==0.1.9
isort==5.13.2
mypy==1.8.0
pre-commit==3.6.0
bandit[toml]==1.7.6

# Optional: Production enhancements
# gunicorn==21.2.0  # Production WSGI server
